<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>تقرير المتابعة الدورية</title>@include('admin.student_follow_ups._report_styles')</head><body><div class="print"><button onclick="window.print()">طباعة التقرير</button></div><main class="report"><header class="head"><div><h1>تقرير المتابعة الدورية</h1><p>{{ optional($school)->name ?: optional($school)->name_en }}</p></div><img class="logo" src="{{ asset('assets/images/school/adham_black.png') }}" alt=""></header><section class="meta"><div><span>المدرس</span><strong>{{ $teacher->first_name }} {{ $teacher->last_name }}</strong></div><div><span>العام الدراسي</span><strong>{{ $year->name }}</strong></div><div><span>الشهر</span><strong>{{ $month }}</strong></div><div><span>عدد الطلاب والسياقات</span><strong>{{ $rows->count() }}</strong></div></section>@forelse($groups as $group)<h2 class="group">{{ $group->first()->class_name }} | {{ $group->first()->room_name }} | {{ $group->first()->lesson_name }}</h2><table><thead><tr><th>الطالب</th><th>النصف الأول</th><th>النصف الثاني</th><th>الحالة</th><th>عدد المتابعات</th></tr></thead><tbody>@foreach($group as $row)<tr><td>{{ $row->student_name }}</td><td>{{ __('student_follow_up_admin.'.$row->half_one_status) }}</td><td>{{ __('student_follow_up_admin.'.$row->half_two_status) }}</td><td>{{ __('student_follow_up_admin.'.$row->monthly_status) }}</td><td>{{ $row->observation_count }}</td></tr>@endforeach</tbody></table>@empty<p>لا توجد بيانات مطابقة.</p>@endforelse</main></body></html>
