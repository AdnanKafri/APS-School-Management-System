@extends('admin.layouts.v2')
@section('page_title', __('student_follow_up_admin.observations'))
@section('page_subtitle', $year->name)
@section('style') @include('admin.student_follow_ups._styles') @endsection
@section('content')<div class="sfu-admin"><div class="v2-card sfu-shell"><div class="sfu-intro"><div><h3>{{ __('student_follow_up_admin.observations') }}</h3><p>{{ __('student_follow_up_admin.month') }}: {{ $month }} · {{ __('student_follow_up_admin.current_year_only') }}</p></div><a class="btn btn-outline-secondary" href="{{ route('admin.student_follow_ups.index', ['month'=>$month]) }}">{{ __('student_follow_up_admin.back') }}</a></div>@forelse($followUps as $followUp)<div class="sfu-observation"><div class="sfu-observation-head"><div><strong>{{ optional($followUp->teacher)->first_name }} {{ optional($followUp->teacher)->last_name }} · {{ optional($followUp->lesson)->name }}</strong><span class="sfu-meta">{{ optional($followUp->classRoom)->name }} / {{ optional($followUp->room)->name }} · {{ optional($followUp->observed_at)->timezone('Asia/Damascus')->format('Y-m-d H:i') }}</span></div>@if($followUp->edited_at)<span class="sfu-badge not_due">{{ __('student_follow_up_admin.edited') }}</span>@endif</div><p class="sfu-note">{{ $followUp->note ?: __('student_follow_up_admin.no_note') }}</p></div>@empty<div class="sfu-empty"><h4>{{ __('student_follow_up_admin.empty_title') }}</h4></div>@endforelse<div class="mt-3">{{ $followUps->links() }}</div></div></div>@endsection
