<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>تقرير المتابعة الدورية للطالب</title>@include('admin.student_follow_ups._report_styles')</head><body><div class="print"><button onclick="window.print()">طباعة التقرير</button></div><main class="report"><header class="head"><div><h1>تقرير المتابعة الدورية للطالب</h1><p>{{ optional($school)->name ?: optional($school)->name_en }}</p></div><img class="logo" src="{{ asset('assets/images/school/adham_black.png') }}" alt=""></header><section class="meta"><div><span>الطالب</span><strong>{{ $student->first_name }} {{ $student->last_name }}</strong></div><div><span>العام الدراسي</span><strong>{{ $year->name }}</strong></div><div><span>الصف والشعبة</span><strong>{{ optional($placement)->class_name ?: 'غير متوفر' }} / {{ optional($placement)->room_name ?: 'غير متوفر' }}</strong></div><div><span>الشهر</span><strong>{{ $month }}</strong></div></section><table><thead><tr><th>المادة</th><th>المدرس</th><th>التاريخ</th><th>الفترة</th><th>التقييم</th><th>الملاحظة</th></tr></thead><tbody>@forelse($followUps as $followUp)<tr><td>{{ optional($followUp->lesson)->name }}</td><td>{{ optional($followUp->teacher)->first_name }} {{ optional($followUp->teacher)->last_name }}</td><td>{{ optional($followUp->observed_at)->timezone('Asia/Damascus')->format('Y-m-d H:i') }}</td><td>{{ $followUp->compliance_half == 1 ? 'النصف الأول' : 'النصف الثاني' }}</td><td>{{ $followUp->level ? __('student_follow_up.levels.'.$followUp->level) : '—' }}</td><td class="note">{{ $followUp->note ?: '—' }}</td></tr>@empty<tr><td colspan="6">لا توجد متابعات مسجلة لهذا الشهر.</td></tr>@endforelse</tbody></table></main></body></html>
