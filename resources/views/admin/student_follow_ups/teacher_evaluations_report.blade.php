<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>سجل التقييمات</title>@include('admin.student_follow_ups._report_styles')</head><body><div class="print"><button onclick="window.print()">طباعة التقرير</button></div><main class="report"><header class="head"><div><h1>سجل التقييمات</h1><p>{{ optional($school)->name ?: optional($school)->name_en }}</p></div><img class="logo" src="{{ asset('assets/images/school/adham_black.png') }}" alt=""></header><section class="meta"><div><span>المدرس</span><strong>{{ $teacher->first_name }} {{ $teacher->last_name }}</strong></div><div><span>العام الدراسي</span><strong>{{ $year->name }}</strong></div><div><span>الشهر</span><strong>{{ $month }}</strong></div><div><span>عدد التقييمات الفعلية</span><strong>{{ $followUps->count() }}</strong></div></section>@forelse($groups as $group)<h2 class="group">{{ optional($group->first()->classRoom)->name }} | {{ optional($group->first()->room)->name }} | {{ optional($group->first()->lesson)->name }}</h2><table><thead><tr><th>الطالب</th><th>التاريخ</th><th>الفترة</th><th>التقييم</th><th>الملاحظة</th></tr></thead><tbody>@foreach($group as $row)<tr><td>{{ optional($students->get($row->student_id))->first_name }} {{ optional($students->get($row->student_id))->last_name }}</td><td>{{ optional($row->observed_at)->timezone('Asia/Damascus')->format('Y-m-d H:i') }}</td><td>{{ $row->compliance_half==1?'النصف الأول':'النصف الثاني' }}</td><td>{{ $row->level?__('student_follow_up.levels.'.$row->level):'—' }}</td><td class="note">{{ $row->note?:'—' }}</td></tr>@endforeach</tbody></table>@empty<p>لا توجد تقييمات فعلية مسجلة لهذا الشهر.</p>@endforelse</main></body></html>
